// ── Firebase project config ──────────────────────────────────────────────
// Follow the "Firebase setup" steps in README.md to get these 6 values
// from your Firebase project (Project settings → General → Your apps → Web app).
// Paste them below, replacing the placeholder strings. Then commit/push this
// file (it is safe to keep these public values in a public repo — they are
// not secret, access is controlled by Firestore security rules instead).

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
